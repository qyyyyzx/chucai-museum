# 楚菜文化数字博物馆 - 团队分工详细指南

> 本文档面向零基础开发人员，按重要性从高到低排列，每个任务都有明确的验收标准。
>
> **最后更新**：2026-09-15

---

## 团队成员

| 姓名 | 角色 | 主要负责 |
|------|------|----------|
| 董文静 | 用户模块 | B1 用户登录 |
| 周欣如 | 用户模块 | B2 用户信息页面 |
| 卫淇悦 | 数据模型 | C1 展陈数据模型、H1 餐厅数据模型 |
| 武千惠 | 基础设施 + 展陈 | A2 通用工具、A3 通用组件、C2 展陈列表页面 |
| 吴若凡 | 展陈 | C3 展陈详情页面、C4 菜品故事卡 |
| 余佳琦 | 平台适配 | D1 H5 Mock 数据 |
| 李语瞳 | 平台适配 + AR | D2 微信云函数、G1 AR 调研 |
| 黄晓 | LBS 模块 | E1 地理位置、H2 楚菜地图页面 |
| 张耀川 | 社交模块 + 文化 | F1 社交打卡、H3 时辰介绍页 |

---

## A. 基础设施搭建（最高优先级，所有人依赖此项）

### A1. 项目初始化负责人（已完成）

**负责人**：武千惠（已完成）

**任务描述**：确保所有人都能把项目跑起来

**具体工作**：
1. 确认 `npm install` 能正常安装依赖
2. 确认 `npm run dev:h5` 能启动开发服务器
3. 确认 `npm test` 能跑通测试
4. 把遇到的问题和解决方案记录到 `docs/setup-faq.md`

**验收标准**：
- [x] 本地能打开 http://localhost:5173 看到页面
- [x] 运行 `npm test` 显示所有测试通过
- [x] 写了一份新人上手指南文档

**预计时间**：1-2 天

---

### A2. 通用工具开发负责人（已完成）

**负责人**：武千惠（已完成）

**任务描述**：开发所有模块都要用的基础工具

**具体工作**：
1. 阅读 `src/shared/llm.md` 了解约束
2. 实现 `src/shared/utils/route-query.js` - 路由参数处理工具
3. 实现 `src/shared/utils/image-encode.js` - 图片转 base64 工具
4. 为每个工具函数编写单元测试，放在 `tests/modules/shared/` 目录

**验收标准**：
- [x] 每个工具函数都有对应的测试文件
- [x] 运行 `npm test` 全部通过
- [x] 工具函数有中文注释说明用途和参数

**预计时间**：3-5 天

---

### A3. 通用组件开发负责人（已完成）

**负责人**：武千惠（已完成）

**任务描述**：开发所有页面都要用的基础 UI 组件

**具体工作**：
1. 阅读 `src/shared/llm.md` 了解约束
2. 开发以下组件（必须使用 `@dcloudio/uni-ui`）：
   - 退出/返回按钮组件（用于 reLaunch/switchTab 页面）
   - 加载状态组件
   - 错误提示组件
3. 每个组件写一个使用示例页面

**验收标准**：
- [x] 组件在 H5 模式下能正常显示
- [x] 没有使用任何 emoji 或装饰性符号
- [x] 所有文本输入使用 `<uni-easyinput>` 而非原生 `<input>`

**预计时间**：3-5 天

---

## B. 用户模块（所有业务模块依赖登录态）

### B1. 用户登录功能负责人

**负责人**：董文静

**任务描述**：实现用户登录和登录态管理

**具体工作**：
1. 阅读 `src/modules/user/llm.md` 了解模块边界
2. 在 `src/modules/user/domain/` 实现用户数据模型
3. 在 `src/modules/user/services/` 实现登录业务逻辑
4. 在 `src/platform/mock/` 实现 H5 端的 mock 登录（用 localStorage 模拟）
5. 在 `cloudfunctions/` 实现微信端的真实登录云函数
6. 编写单元测试

**验收标准**：
- [ ] H5 模式下能模拟登录，刷新页面后登录态保持
- [ ] mock 和云函数的接口签名完全一致（方法名、参数、返回值结构相同）
- [ ] 有至少 3 个测试用例（登录成功、登录失败、获取用户信息）

**预计时间**：5-7 天

---

### B2. 用户信息页面负责人

**负责人**：周欣如

**任务描述**：开发用户个人信息展示和编辑页面

**具体工作**：
1. 阅读 `src/pages/llm.md` 了解页面归属规则
2. 创建页面文件 `src/pages/user/profile.vue`
3. 在 `src/pages.json` 中注册页面路由
4. 使用 uni-ui 组件展示用户头像、昵称等信息
5. 实现编辑功能（使用 `<uni-easyinput>`）

**验收标准**：
- [ ] 页面能正常显示用户信息
- [ ] 编辑后能保存（H5 模式保存到 localStorage）
- [ ] 没有使用原生 `<input>` 或 `<textarea>`
- [ ] 没有使用任何 emoji

**预计时间**：3-5 天

---

## C. 数字展陈模块（核心业务功能）

### C1. 展陈数据模型负责人（已完成）

**负责人**：卫淇悦（已完成）

**任务描述**：定义楚菜展品的数据结构

**具体工作**：
1. 阅读 `src/modules/exhibit/llm.md` 了解模块边界
2. 在 `src/modules/exhibit/domain/` 定义数据模型：
   - 菜品（名称、图片、历史、做法、食材）
   - 名厨（姓名、照片、简介、代表菜）
   - 历史时期（朝代、特点、代表菜）
3. 编写数据模型的验证函数和测试

**验收标准**：
- [x] 数据模型有完整的 JSDoc 注释
- [x] 验证函数能检测必填字段缺失
- [x] 测试覆盖正常情况和异常情况

**预计时间**：3-5 天

---

### C2. 展陈列表页面负责人

**负责人**：武千惠

**任务描述**：开发菜品/名厨的列表展示页面

**具体工作**：
1. 创建页面 `src/pages/exhibit/list.vue`
2. 使用 `<uni-list>` 或 `<uni-card>` 组件展示列表
3. 实现下拉刷新和上拉加载更多
4. 点击列表项跳转到详情页

**验收标准**：
- [x] 列表能正常显示 mock 数据
- [x] 下拉刷新能获取新数据
- [x] 点击能正确跳转到详情页
- [x] 页面使用 uni-ui 组件，无手写符号

**预计时间**：5-7 天

---

### C3. 展陈详情页面负责人

**负责人**：吴若凡

**任务描述**：开发单个菜品/名厨的详情展示页面

**具体工作**：
1. 创建页面 `src/pages/exhibit/detail.vue`
2. 接收路由参数（菜品 ID）
3. 使用 `pickRouteParam()` 获取参数（解决 query 丢失问题）
4. 展示图文混排的详情内容

**验收标准**：
- [x] 从列表页点击能正确显示对应详情
- [x] 图片能正常加载显示
- [x] 使用 `pickRouteParam()` 而非直接读 `options.id`

**预计时间**：3-5 天

---

### C4. 菜品故事卡负责人

**负责人**：吴若凡

**任务描述**：以图文卡片形式展示楚菜菜品的历史典故与制作技艺

**具体工作**：
1. 在 `src/modules/exhibit/domain/` 扩展菜品数据模型，增加故事字段：
   - `story`：历史典故文本（200-500字）
   - `technique`：制作技艺描述
   - `storyImages`：典故配图数组（1-3张）
2. 创建页面 `src/pages/exhibit/story.vue`，实现故事卡展示：
   - 顶部大图轮播（典故配图）
   - 菜品名称 + 历史典故正文
   - 制作技艺折叠面板（点击展开/收起）
   - 底部"了解更多"跳转到菜品详情页（C3）
3. 在展陈列表页（C2）或详情页（C3）中增加"查看故事"入口
4. 在 `src/pages.json` 中注册路由

**验收标准**：
- [x] 故事卡页面能正确展示菜品典故和配图
- [x] 制作技艺支持折叠/展开
- [x] 从列表页或详情页能正确跳转到故事卡
- [x] 使用 `pickRouteParam()` 获取路由参数
- [x] 页面使用 uni-ui 组件，无 emoji

**前置依赖**：C1（数据模型）、C2（列表页，用于入口跳转）

**预计时间**：3-5 天

---

## D. 平台适配层（保证两端一致性）

### D1. H5 Mock 数据负责人

**负责人**：余佳琦

**任务描述**：为所有业务接口提供 H5 端的模拟数据

**具体工作**：
1. 阅读 `src/platform/llm.md` 了解平台层约束
2. 在 `src/platform/mock/` 下为每个模块创建 mock 实现
3. 使用 localStorage 模拟数据持久化
4. 准备至少 10 条示例菜品数据、5 条名厨数据

**验收标准**：
- [ ] 所有接口返回的数据结构与云函数一致
- [ ] 刷新页面后数据不丢失（localStorage 持久化）
- [ ] mock 数据包含中文内容，符合楚菜文化主题

**预计时间**：5-7 天

---

### D2. 微信云函数负责人

**负责人**：李语瞳

**任务描述**：实现微信小程序端的云函数

**具体工作**：
1. 阅读 `cloudfunctions/llm.md` 了解云函数约束
2. 在 `cloudfunctions/` 下创建云函数：
   - `user-login` - 用户登录
   - `exhibit-list` - 获取展品列表
   - `exhibit-detail` - 获取展品详情
3. 配置云数据库集合和权限

**验收标准**：
- [ ] 云函数在微信开发者工具中能本地调试通过
- [ ] 接口签名与 H5 mock 完全一致
- [ ] 云函数有错误处理（参数校验、数据库异常）

**预计时间**：7-10 天

---

## E. LBS 推荐模块

### E1. 地理位置功能负责人

**负责人**：黄晓

**任务描述**：实现基于位置的餐厅推荐

**具体工作**：
1. 阅读 `src/modules/lbs/llm.md` 了解模块边界
2. 调用 `uni.getLocation()` 获取用户位置
3. 实现距离计算函数（经纬度转距离）
4. 创建页面 `src/pages/lbs/nearby.vue`

**验收标准**：
- [ ] H5 模式下能模拟获取位置
- [ ] 能计算两点之间的距离
- [ ] 列表按距离排序显示

**预计时间**：5-7 天

---

## F. 社交打卡模块

### F1. 打卡功能负责人

**负责人**：张耀川

**任务描述**：实现拍照打卡和分享功能

**具体工作**：
1. 阅读 `src/modules/social/llm.md` 了解模块边界
2. 调用 `uni.chooseImage()` 选择照片
3. 使用 `blobToDataURL()` 转换图片格式（解决跨页面问题）
4. 创建打卡记录页面 `src/pages/social/checkin.vue`

**验收标准**：
- [x] 能选择照片并预览
- [x] 打卡记录能保存到 localStorage
- [x] 图片使用 base64 格式存储，不使用 blob URL

**预计时间**：5-7 天

---

## G. AR 菜品预览模块（技术难度最高，可选）

### G1. AR 功能调研负责人

**负责人**：李语瞳

**任务描述**：调研 uni-app 中实现 AR 的可行方案

**具体工作**：
1. 阅读 `src/modules/ar/llm.md` 了解模块边界
2. 调研 uni-app 支持的 AR 方案（如 AR.js、Vuforia 等）
3. 编写技术调研文档 `docs/ar-research.md`
4. 实现一个最小可行的 AR demo

**验收标准**：
- [ ] 调研文档列出至少 3 种方案的优缺点
- [ ] demo 能在真机或模拟器上运行
- [ ] 明确标注哪些功能需要原生插件支持

**预计时间**：7-10 天（调研+demo）

---

## H. "筷乐寻楚"楚菜地图（特色功能）

> 以"荆州十二时辰"节目表演为时间轴，展示对应时段的推荐餐厅，点击显示地址、营业时间、招牌菜品。

### H1. 餐厅数据模型负责人

**负责人**：卫淇悦

**任务描述**：定义餐厅数据结构并准备 mock 数据

**具体工作**：
1. 在 `src/modules/lbs/domain/` 定义餐厅数据模型：
   - 餐厅基础信息：`id`、`name`、`address`、`phone`、`coordinates`（经纬度）
   - 营业信息：`businessHours`（营业时间，含时段标记）
   - 菜品信息：`signatureDishes`（招牌菜品数组，关联菜品ID）
   - 时间段关联：`timeSlot`（对应荆州十二时辰的时段标识）
2. 准备 mock 数据：
   - 至少 12 家餐厅，每家对应一个时辰时段
   - 每家餐厅至少 3 道招牌菜品
   - 地址覆盖荆州主要区域
3. 在 `src/platform/mock/` 实现 `getRestaurantsByTimeSlot()` 接口
4. 编写数据验证函数和测试

**验收标准**：
- [x] 数据模型有完整的 JSDoc 注释
- [x] mock 数据包含 12 个时辰时段的餐厅
- [x] 接口支持按时段筛选和全量查询两种模式
- [x] 测试覆盖正常查询和空结果情况

**前置依赖**：无（可与 C1 并行）

**预计时间**：3-5 天

---

### H2. 楚菜地图页面负责人

**负责人**：黄晓

**任务描述**：开发"筷乐寻楚"地图主页面，实现时间轴交互

**具体工作**：
1. 创建页面 `src/pages/lbs/chumap.vue`
2. 实现"荆州十二时辰"时间轴组件：
   - 横向滚动的时间轴（子、丑、寅、卯...亥）
   - 每个时辰显示对应名称和时段（如"辰时 · 食时 07:00-09:00"）
   - 点击时辰切换对应餐厅列表
3. 餐厅列表展示：
   - 使用 `<uni-card>` 展示餐厅卡片
   - 每张卡片显示：餐厅名称、距离、招牌菜品预览（前2道）
   - 点击卡片展开详情（地址、营业时间、全部招牌菜品）
4. 展开详情使用 `<uni-popup>` 弹窗：
   - 餐厅地址 + 一键导航按钮（调用 `uni.openLocation()`）
   - 营业时间表格
   - 招牌菜品列表（可点击跳转到菜品详情页）
5. 在 `src/pages.json` 中注册路由

**验收标准**：
- [ ] 时间轴能正确切换 12 个时辰
- [ ] 切换时辰后餐厅列表自动更新
- [ ] 点击餐厅卡片能展开详情弹窗
- [ ] 导航按钮能调起地图应用
- [ ] 招牌菜品能跳转到菜品详情
- [ ] 页面使用 uni-ui 组件，无 emoji

**前置依赖**：H1（餐厅数据）、E1（地理位置，用于距离计算）

**预计时间**：5-7 天

---

### H3. 时辰介绍页负责人

**负责人**：张耀川

**任务描述**：为每个时辰编写文化介绍，丰富地图功能的文化内涵

**具体工作**：
1. 在 `src/modules/lbs/domain/` 定义时辰数据模型：
   - `timeSlot`：时辰标识（zi/chou/yin/mao...）
   - `name`：时辰名称（子时、丑时...）
   - `alias`：别名（夜半、鸡鸣...）
   - `period`：时段范围（23:00-01:00...）
   - `description`：文化介绍文本
   - `image`：时辰主题配图
2. 创建时辰详情弹窗或页面组件
3. 在地图页面（H2）的时间轴上增加"查看详情"入口

**验收标准**：
- [ ] 每个时辰都有对应的文化介绍
- [ ] 介绍内容包含时辰名称、别名、时段、文化描述
- [ ] 配图能正常显示

**前置依赖**：H2（地图页面）

**预计时间**：2-3 天

---

## 通用规范（所有人必须遵守）

### 提交代码前检查清单

1. **运行测试**：`npm test` 必须全部通过
2. **检查 emoji**：`grep -rP '[●✓✗★☆→←↑↓▲▼◆◇■□◉◎♥♡⚠⚡]' src` 必须零匹配
3. **检查原生输入框**：不能有裸的 `<input>` 或 `<textarea>`
4. **文档同步**：修改了代码必须更新对应的 `llm.md`
5. **提交信息格式**：`[type][你的名字] 具体改动内容`

### 分支规范

1. 禁止在 main 分支直接开发
2. 从 main 拉新分支：`git checkout -b feat/你的功能名`
3. 完成后通过 Merge Request 合入 main

### 遇到问题怎么办

1. 先看对应模块的 `llm.md` 文档
2. 搜索项目中的类似代码参考
3. 记录问题到 `docs/issues.md`，开会讨论

---

## 时间线总览

| 阶段 | 任务 | 负责人 | 预计时间 | 依赖 |
|------|------|--------|----------|------|
| 第 1 周 | A1 项目初始化 | 武千惠（已完成） | 2 天 | 无 |
| 第 1-2 周 | A2 通用工具 | 武千惠 | 3-5 天 | A1 |
| 第 2 周 | A3 通用组件 + B1 用户登录 | 武千惠 + 董文静 | 5 天 | A1 |
| 第 3 周 | B2 用户页面 + C1 数据模型 + D1 Mock数据 + H1 餐厅数据模型 | 周欣如 + 卫淇悦 + 余佳琦 + 卫淇悦 | 5 天 | B1 |
| 第 4 周 | C2 列表页 + C3 详情页 + C4 故事卡 | 武千惠 + 吴若凡 + 吴若凡 | 5 天 | C1, D1 |
| 第 5 周 | D2 云函数 + E1 LBS + F1 打卡 + H2 楚菜地图 | 李语瞳 + 黄晓 + 张耀川 + 黄晓 | 5 天 | C2, H1 |
| 第 6 周 | H3 时辰介绍 + G1 AR调研 + 联调测试 | 张耀川 + 李语瞳 + 全员 | 5 天 | 全部 |

---

## 每日站会模板

每人每天回答三个问题：
1. 昨天完成了什么？
2. 今天计划做什么？
3. 遇到了什么阻塞？

---

## 联系方式

遇到问题及时沟通，不要卡住超过 2 小时不求助。
